import { DirectivaxDirective } from './directivax.directive';

describe('DirectivaxDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaxDirective();
    expect(directive).toBeTruthy();
  });
});
