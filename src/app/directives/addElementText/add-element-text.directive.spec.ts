import { AddElementTextDirective } from './add-element-text.directive';

describe('AddElementTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AddElementTextDirective();
    expect(directive).toBeTruthy();
  });
});
