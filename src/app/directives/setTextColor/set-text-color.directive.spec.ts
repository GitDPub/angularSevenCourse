import { SetTextColorDirective } from './set-text-color.directive';

describe('SetTextColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetTextColorDirective();
    expect(directive).toBeTruthy();
  });
});
