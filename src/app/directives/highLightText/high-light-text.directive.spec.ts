import { HighLightTextDirective } from './high-light-text.directive';

describe('HighLightTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightTextDirective();
    expect(directive).toBeTruthy();
  });
});
